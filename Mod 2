# Assignment 2: Function Debugging and myMean Evaluation

# 1. Vector assignment
assignment2 <- c(16, 18, 14, 22, 27, 17, 19, 17, 17, 22, 20, 22)

# 2. Buggy Function Definition
myMean <- function(assignment2) {
  return(sum(assignment) / length(someData))
}

# Testing original function:
# Output: Error in sum(assignment) : object 'assignment' not found

# 3. Corrected Function Definition
myMean_fixed <- function(x) {
  return(sum(x) / length(x))
}

# Testing corrected function:
myMean_fixed(assignment2)
# Output: [1] 19.41667
